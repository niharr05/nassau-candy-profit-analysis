# Environments
.env
.venv
env/
venv/
ENV/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.pytest_cache/

# Data & Notebooks
# *.csv
# However, if this is a portfolio repository and the CSV is tiny/sample data, 
# you typically commit it. Since the prompt tells us we MUST commit the CSV for evaluators, 
# I will explicitly NOT ignore the CSV, or specifically ignore other CSVs.
# We will comment this out to keep Nassau Candy Distributor.csv tracked.
# *.csv

# Streamlit
.streamlit/
# But wait, earlier we wanted to remove it. Actually ignoring it is fine.

# Charts
nassau_charts.png
nassau_chart_*.png

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
